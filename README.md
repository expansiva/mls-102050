# 102050 · cafeFlow (scaffold only)

Part of **collab.codes**.

`102050` has **no source on disk** — only `l2/project.ts`,
`l2/designSystem.ts` and `l5/project.json`. No `l1`, no `l4`, no page.

## Declared configuration

`l5/project.json` declares module `cafeFlow`, a single design system
(`default`) and one dependency:

```json
"dependencies": [{ "projectId": "102034", "kind": "mdm-infrastructure" }]
```

Last activity: 2026-08-21.

## Careful

The cafeFlow **application** is [`102051`](../mls-102051). This project only
carries the name.
